/**
 * Re-export createDefaultWidget from the local studioTypes module.
 * The implementation lives in models/studioTypes.ts alongside the data types.
 */
export { createDefaultWidget } from './models/studioTypes';
