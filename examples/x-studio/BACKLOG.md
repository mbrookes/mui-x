EBL-01: Currently orders have a currency symbol, but the currency is assumed to be consistent (for agregation, display in tables etc. Perhaps need a currency converted column. To be researched.)

**Researched — deferred.** The data already has `currency` per order (USD, EUR, GBP, etc.) and the calculated-fields system can define a `normalizedTotal = total * exchangeRate` expression field per source. To implement properly this would need: (1) an exchange-rates data source bundled with the demo, (2) a many-to-one relationship orders → exchange-rates (keyed by currency), and (3) a calculated column `expr-total-usd = total * rate`. The infrastructure is all in place but it's an exercise in data modelling rather than new platform features. Deferred until the exchange-rate data source is prepared.

✅ EBL-02: Change the language selection in the example app settings dialog to a select.

**Fixed** — converted the language control in `examples/x-studio/src/components/SettingsDialog.tsx` from a `RadioGroup` to an MUI `Select` (same options/default/wiring). (x-studio-composed has the same control as a RadioGroup; left untouched — separate concern.)

✅ EBL-03: CRM Pipeline page -> Deals by Stage widget: The funnel shows qualification as 105% of total. Prospecting is being considered the total, whereas it's only the number of deals in that stage. If we assume That any deal lower down the funnel was once in the stage above, should we roll up the totals? Or is there another better way of calculating/displaying the percentage? We probably still need to distingish between deals in stage and deals moved to the next stage. Consider best practice for this.

**Fixed** — implemented Option B (generator fix) from `EBL-03-04-PROPOSAL.md`. The generator now assigns deals a **furthest-reached depth** drawn from a decreasing distribution (`REACH_DEPTH_WEIGHTS`), so cumulative "reached ≥ stage k" counts fall off monotonically by construction — the funnel can never exceed 100%. New deal fields: `stageReached` (numeric depth, hidden), `owner` (6-rep roster), `daysInStage`, and `stageTimeline` (per-stage entry/exit dates). `aggregateFunnelReached()` in `chartAggregation.ts` computes cumulative reached counts, step-conversion fractions, and a separate Closed Lost exit total. `StudioFunnelChart` clamps `widthPct ≤ 1`, headlines step-conversion ("▼ −x%"), keeps "% of total" secondary, shows "currently in stage: N" tooltip, and renders Closed Lost as a separate exit stat. `widget-chart6-deals-by-stage` updated to use cumulative reached mode.

✅ EBL-04: Related to EBL-03: Should we show where in each step a deal was lost? Would a waterfall chart be better? (I don't know.) Or a heatmap showing time in stage by some other category? (does one category perform better than another?) Or something else? Be creative.

**Fixed** — added two new widgets to page 6 (CRM Pipeline): (1) `widget-chart6-stage-conversion` — a step-conversion bar chart showing the % of deals that advanced to each next stage (directly answers "where are deals lost?", renderable from the same cumulative counts as EBL-03); (2) `widget-chart6-time-in-stage` — a heatmap of stage × owner coloured by avg `daysInStage`, answering "does one owner/segment stall in a stage?" (approximation: reads one row per deal, so `daysInStage` is days in the current stage, not a full per-stage matrix — noted in config comment). See `EBL-03-04-PROPOSAL.md` for the full analysis and option comparison.

✅ EBL-05: Update the custom banner widget such that: 
- The banner is the entire widget, no title.
- The widget description makes it clear that it's a custom widget.
- The widget accepts a value field that determines its severity.
- That the value can be from some specific time range, so for example if some value exceeded a threshold in the last day.
- If possible, control whether the widget is displayed at all, based on some condidition.

**Fixed** — `AlertBannerWidget`/`AlertBannerSetupPanel` reworked: full-bleed banner covering the card; description now marks it as an example custom widget; configurable value field + aggregation (sum/avg/max/min/count) + thresholds drive the severity (success/warning/error/info); a date-field + lookback-days window scopes the value to a time range (anchored to the max date in the demo data, which is historical); a `hideBelow` condition returns `null` in view mode when severity is below the threshold. Demo instance added to `salesDashboard.ts`. **"No title" fully addressed via a package change:** added a `fullBleed?: boolean` flag to `StudioCustomWidgetDef` — when set, `StudioWidgetCard` omits the title/subtitle header entirely (DOM + aria node) and drops the card padding so the component renders edge-to-edge. The banner def sets `fullBleed: true`. **Remaining minor limitations:** the card's outer border/chrome still belongs to the framework (the `hideBelow` condition hides content, not the card outline — and #5 was an "if possible" requirement); `DataSourceFieldSelect` isn't exported from the public API so the panel uses a plain MUI `Select`; severity falls back to `info` in adapter/server mode (no client rows).

✅ EBL-06: CRM Contacts page > Total Contacts has a value, but th ewidget config doesn't show any fileds selected. Ensure that the data and widget config correctly reflects the dashboard state, and that all of example widgets can be created from scratch by the user. This may require changes to the x-studio package.

**Fixed** — root cause: `widget-kpi7-total-contacts` used `kpiValueField: 'id'` + `kpiAggregation: 'count'`, but `id` is a hidden field; `count` ignores the field (so a value rendered), while `KpiSetupPanel` builds its picker from non-hidden fields only (so the picker showed nothing) — making the widget un-reproducible. Fieldless count is now a first-class, reproducible state: `StudioKpiWidget` renders the row tally when count has no value field; `KpiSetupPanel` gained a data-source picker and locks the aggregation to an explicit, disabled "Count" when no value field is selected. `salesDashboard.ts`: Total Contacts now has no `kpiValueField`; two sibling count KPIs repointed off hidden `id` to visible string fields (count ignores them, but the picker shows a real selection and trend/sparkline still work). **Audit follow-through:** the chart widgets with the same `yField:'id'`+count pattern were repointed off hidden `id` to a visible numeric field (count ignores the value, so results are unchanged but the config is now reproducible) — `widget-chart5-order-funnel` → `total`, and `widget-chart8-activities-by-type` / `-by-outcome` / `-by-department` → `durationMin`. **Two cases deliberately left:** (1) `widget-chart7-contacts-by-dept` / `-by-role` — the CRM Contacts source has **no visible numeric field**, so there's nothing to repoint to; making these reproducible needs the same package-level treatment as KPIs (a first-class fieldless count for charts in `ChartSetupPanel`) — a follow-up. (2) `widget-chart6-deals-by-stage` — left untouched because it's the widget under EBL-03/04 review. The grid widgets (columns on hidden `id`/`customerId`) remain a genuine design call (unhide vs drop the column).
