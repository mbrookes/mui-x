'use client';

/**
 * Returns the chart colour palette for the dashboard.
 * Chart colours are determined solely by the app's MUI theme.
 */
export function usePageChartColors(): string[] | undefined {
  return undefined;
}
