// StudioAIToolName is defined in `@mui/x-studio-schema`'s `aiTypes.ts`. This file is a
// thin re-export shim so existing imports from `./studioAITools` keep working.
export type { StudioAIToolName } from '../../models/aiTypes';
