# Vega-Lite → MUI X Charts: support matrix and gaps

This document tracks what the wrapper translates natively, what renders with a visible approximation, and what cannot be expressed with `@mui/x-charts` components in any tier. The wrapper depends on `@mui/x-charts-premium` (which re-exports Pro and MIT): Premium-covered marks render with a watermark until a license key is set. Runtime code reports the same information per-spec through the `onGaps` prop as `TranslationGap` objects.

## Severity levels

- **✅ supported** — feature translates to native x-charts behavior.
- **⚠️ partial/approximated** — feature renders with a visible approximation or reduced functionality.
- **✅ supported (Premium)** — translates natively via `@mui/x-charts-pro`/`-premium` components (watermarked without a license key).
- **❌ unsupported (all tiers)** — no x-charts equivalent in any tier.

---

## Marks

All fifteen translatable mark kinds are implemented (`src/marks/*.ts`, dispatched from `markRegistry` in `src/marks/index.ts`): the Premium-tier `rect` (heatmap), ranged `bar` (rangeBar), and `geoshape` (map) render through `@mui/x-charts-premium` (watermarked without a license key), and `boxplot`/`errorbar`/`errorband`/`text`/`image` render through the custom overlay pipeline (`src/overlays` — plain SVG surface children built on the public `useXScale`/`useYScale` hooks, the same composition pattern as `ChartsReferenceLine`). Unknown mark strings fall through to a generic `unsupported` gap.

| Vega-Lite mark           | Status                  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| :----------------------- | :---------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bar`                    | ✅ supported            | `marks/bar.ts` → `bar` series + `BarPlot`. Vertical (band x, quantitative y) and horizontal (auto-inferred, or explicit `mark.orient`, → `layout: 'horizontal'`). Color-field splitting into one series per group, stacked by default (`stack: 'zero'`/`'normalize'`→`stackOffset: 'expand'`/`'center'`→`'silhouette'`), or grouped (unstacked) when `xOffset`/`yOffset` is present. Pairing the value channel with its `2` twin (`y`+`y2` / `x`+`x2` field pairs) emits a Premium `rangeBar` series instead (watermarked without a license). `mark.cornerRadius`/`mark.size`/`mark.binSpacing` are recognized but have no effect (see the Config section below).                                                                                                                                                                                                                         |
| `line`                   | ⚠️ partial/approximated | `marks/lineArea.ts` → `line` series + `LinePlot`. Index-aligned to `x`'s discrete category domain; missing cells are `null` and `connectNulls` is always `false`. Color-field splitting; `mark.interpolate` mapped where an exact `CurveType` exists ('linear', 'monotone'→'monotoneX', 'natural', 'step'/'step-before'/'step-after'), else approximated to `'catmullRom'` with a `partial` gap ('basis'/'bundle'/'cardinal'/'catmull-rom') or falls back to `'linear'` with a gap for anything else. `mark.point` → `showMark` + `MarkPlot` overlay. Temporal `x` values are coerced to `Date` before category alignment (regression-tested in the golden suite).                                                                                                                                                                                                                        |
| `area`                   | ⚠️ partial/approximated | Same compiler as `line` (`marks/lineArea.ts`) with `area: true` + `AreaPlot`; stacks by default when color-split (Vega-Lite's `stack: 'zero'` default for areas). Subject to the same interpolate-approximation notes as `line`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `point` / `circle`       | ⚠️ partial/approximated | `marks/point.ts` → `scatter` series. Both positional channels quantitative/temporal → scatter data; one categorical channel → strip plot against the band/point axis (temporal/nominal/ordinal values are coerced via `resolveAxisValue`, unlike `line`/`area`). Color-field splitting. `mark.size` (static) is approximated via `Math.sqrt(size)` into `markerSize` (`partial` gap); a field-driven `encoding.size` maps to per-point `sizeValue` + a zAxis `sizeMap` (real bubble charts). `shape`/`filled: false`/opacity are ignored.                                                                                                                                                                                                                                                                                                                                                 |
| `square`                 | ⚠️ partial/approximated | Same compiler as `point`; renders as the standard circular scatter marker — the square shape is dropped (`ignored` gap).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tick`                   | ✅ supported            | `marks/point.ts` → a `{kind: 'segments'}` overlay: short perpendicular line segments at each position (rendered by `overlays/Segments.tsx`), no longer circular-point approximations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `trail`                  | ⚠️ partial/approximated | Compiled by `marks/lineArea.ts` as a plain `line` series (`partial` gap: `trail`'s per-point stroke-width-by-field has no x-charts equivalent, so the line renders with uniform width). Subject to the same temporal-axis bug as `line`/`area`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `arc`                    | ✅ supported            | `marks/arc.ts` → `pie` series + `PiePlot`. `theta` (quantitative, aggregated or not) → slice `value`; falls back to a quantitative `y` with a `partial` gap when `theta` is absent. `color` field → slice `label` (one datum per distinct value) and slice color (from an explicit `scale.range`, positionally matched to `scale.domain` or first-appearance order). `mark.innerRadius`/`outerRadius`/`cornerRadius` map directly; `mark.padAngle` (radians) is converted to `paddingAngle` (degrees). No cartesian axes are rendered for a polar chart. **Not** covered: the `radius`/`radius2`/`theta2` _encoding channels_ (as opposed to the `mark.innerRadius`/`outerRadius` _mark_ properties, which are wired) — see the Encoding channels table. Text labels (`encoding.text`) and slice ordering (`encoding.order`) are not applied.                                             |
| `rule`                   | ⚠️ partial/approximated | `marks/rule.ts` → one or more `ChartsReferenceLine`s (not a mark/series). Only `y` (datum, aggregated, or per-distinct-value field) → horizontal line(s); only `x` → vertical line(s), capped at 10 lines with a `partial` gap beyond that. Mark `color`/`strokeWidth`/`strokeDash` → `lineStyle`. A rule with _both_ `x` and `y` (no `x2`/`y2`) is approximated as two full crossing reference lines instead of a single point marker (`partial` gap) — spanning `x`→`x2` or `y`→`y2` segment rules are dropped entirely (`unsupported` gap; no x-charts range/segment primitive exists).                                                                                                                                                                                                                                                                                                |
| `rect`                   | ✅ supported (Premium)  | `marks/rect.ts` → `heatmap` series + `HeatmapPlot` (from `@mui/x-charts-pro`; watermarked without a license). Needs two discrete positional channels and a quantitative color/fill channel (the cell value); the color scale becomes a `zAxis` `colorMap` (continuous, or piecewise when binned). Ranged rects (`x2`/`y2` spans) remain `unsupported`; per-cell stroke/opacity styling is `ignored`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `geoshape`               | ✅ supported (Premium)  | `marks/geoshape.ts` → the `@mui/x-charts-premium` Map (`GeoDataPlot` + `MapShapePlot` inside `ChartsGeoDataProviderPremium`; watermarked without a license). GeoJSON features come from the unit's rows (Feature rows or a single FeatureCollection). Outline maps render `GeoDataPlot` only; a color/fill field over feature properties produces a `mapShape` choropleth series with per-entry colors. TopoJSON topologies convert via `data.format.type: 'topojson'` + `format.feature`; inline `lookup` transforms join tabular values onto features; quantitative/temporal color fields produce a real color (z) axis with a `ContinuousColorLegend`/`PiecewiseColorLegend`; projection `rotate`/`scale`/`translate` forward to the provider. Not covered: sphere/graticule generators, `center`/`parallels`/`clipAngle`/`fit` projection params, `lookup` from `url`/named datasets. |
| `boxplot`                | ✅ supported (overlay)  | `marks/boxplot.ts` → `{kind: 'boxes'}` overlay (`overlays/BoxPlot.tsx`): quartiles via the shared aggregate ops, whiskers per `mark.extent` (default 1.5×IQR clamped to in-fence data; `'min-max'`; numeric k), outlier dots, both orientations. Color-field splits collapse to one box per category (`partial`); sub-mark styling is `ignored`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `errorbar` / `errorband` | ✅ supported (overlay)  | `marks/errorBar.ts` → `{kind: 'errorBars'}` / `{kind: 'band'}` overlays (`overlays/ErrorBars.tsx`): per-category intervals per `mark.extent` ('stderr' default, 'stdev', 'iqr', 'ci' approximated as mean ± 1.96×stderr — `partial`). Errorbar supports both orientations; a transposed band is `partial`-gapped. Typically layered with line/point marks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `text`                   | ✅ supported (overlay)  | `marks/textMark.ts` → `{kind: 'text'}` overlay (`overlays/TextMarks.tsx`): per-row SVG labels with dx/dy offsets and fontSize/font/weight/color/align/baseline styling. `format` strings render unformatted (`partial`); `condition` branches are dropped (`unsupported`). On arc marks, `encoding.text` maps to the pie series' `arcLabel` instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `image`                  | ✅ supported (overlay)  | `marks/imageMark.ts` → `{kind: 'image'}` overlay: `<image href>` per row from the `url` channel, `mark.width`/`height` (default 20px). `aspect` (preserve ratio) is `ignored` — images stretch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

---

## Encoding channels

Not all channels are utilized by all mark compilers; the table below lists what is _recognized_ by the wrapper and how it is handled. Mark-compiler implementation determines the final rendering.

| Channel                    | Status                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :------------------------- | :-------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `x`                        | ✅ supported                                              | Positional. Band/point scale for categorical/temporal values (temporal channels are approximated as a discrete scale — see Scales & axes); linear/log/pow/sqrt/symlog for quantitative.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `y`                        | ✅ supported                                              | Positional. Same scale handling as `x`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `x2`                       | ⚠️ partial (Premium for bars)                             | On horizontal bar marks, `x` + `x2` field pairs translate to a Premium `rangeBar` series (`marks/bar.ts`; watermarked without a license). Rule marks report their own segment gaps. All other marks drop the second endpoint with a `channel:x2` `unsupported` gap (`compile/scales.ts`).                                                                                                                                                                                                                                                                                                                                                          |
| `y2`                       | ⚠️ partial (Premium for bars)                             | Same as `x2`, transposed: vertical bar marks with `y` + `y2` field pairs become `rangeBar` series; other marks drop the endpoint with `channel:y2`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `xOffset` / `yOffset`      | ⚠️ partial                                                | Only `marks/bar.ts` reads these (`xOffset` for vertical bars, `yOffset` for horizontal bars) to switch a color-split bar series from stacked to grouped; a gap (`encoding:bar-offset-mismatch`, `partial`) is reported if the offset field differs from the color field. Every other mark compiler never reads `xOffset`/`yOffset` at all — the channel is silently ignored with no gap on non-bar marks.                                                                                                                                                                                                                                          |
| `color`                    | ⚠️ partial                                                | `compile/color.ts` (`resolveColor`). Nominal/ordinal field → one series per distinct value (in `scale.domain` order when given, else first-appearance order), with `scale.range` or a mapped `scale.scheme` name (see the categorical scheme table in `color.ts`) supplying colors; unmapped scheme names fall back to the chart's default palette with a `partial` gap. Quantitative/temporal field → a continuous (or, when `bin` is set, piecewise) axis `colorMap` computed from the data extent, always with a `partial` gap since only some x-charts series types honor axis `colorMap`. Static color via a `value` def is applied directly. |
| `fill` / `stroke`          | ⚠️ partial                                                | `resolveColor` treats `color` ?? `fill` ?? `stroke` as the color source (Vega shorthand precedence); if both `fill` and `stroke` are given, `stroke` is dropped with an `ignored` gap (`encoding:color-fill-stroke-conflict`).                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `opacity`                  | ⚠️ ignored / unsupported                                  | Never affects rendering. A field-driven `opacity`/`fillOpacity` is reported `unsupported` (`encoding:opacity-field-unsupported` in `color.ts`, `encoding:opacity` in `marks/point.ts`); mark-level static `opacity`/`fillOpacity`/`strokeOpacity` on line/area marks is reported `ignored` (`mark:opacity` in `marks/lineArea.ts`). No mark compiler applies opacity to a series.                                                                                                                                                                                                                                                                  |
| `size`                     | ✅ supported (points)                                     | `marks/point.ts`: a field-driven `encoding.size` maps to per-point `sizeValue` plus a zAxis `sizeMap` (real bubble charts via x-charts' size scale); a static `mark.size` is approximated into `markerSize` via `Math.sqrt(size)` (`partial`). Not read by other mark compilers.                                                                                                                                                                                                                                                                                                                                                                   |
| `shape`                    | ❌ unsupported (all tiers)                                | `marks/point.ts` reports an `ignored` gap (`encoding:shape`); x-charts scatter markers are visually uniform.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `angle`                    | ❌ unsupported (all tiers)                                | Declared in the type but never read anywhere in the compiler; no mark or gap references it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `theta` / `theta2`         | `theta` ✅ supported, `theta2` ❌ unsupported (all tiers) | `marks/arc.ts`: `theta` (field, `datum`, or static `value`) → slice `value`, with a `partial` fallback to a quantitative `y` when absent. `theta2` is read only to fire an `unsupported` gap (`encoding:arc-theta2`); a single pie series cannot express a per-slice start/end radius or angle range.                                                                                                                                                                                                                                                                                                                                              |
| `radius` / `radius2`       | ❌ unsupported (all tiers)                                | These are the _encoding channels_ (as opposed to `mark.innerRadius`/`mark.outerRadius`, which ARE supported — see the Marks table). `marks/arc.ts` reads `radius`/`radius2` only to fire `unsupported` gaps (`encoding:arc-radius`, `encoding:arc-radius2`); a single pie series has one inner/outer radius, not a per-slice value.                                                                                                                                                                                                                                                                                                                |
| `detail`                   | ❌ unsupported (all tiers)                                | Declared in the type but never read anywhere in the compiler; grouping-without-encoding has no x-charts equivalent and produces no gap (silently ignored).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `order`                    | ⚠️ ignored                                                | Only `marks/arc.ts` reads `order`, to fire an `ignored` gap (`encoding:arc-order`) — pie slices keep row order instead. Not read by any other mark compiler (bar/line/point series order follows color-group iteration order, not an `order` channel).                                                                                                                                                                                                                                                                                                                                                                                             |
| `text`                     | ✅ supported                                              | `marks/textMark.ts` renders text marks as overlays; `marks/arc.ts` maps `encoding.text` to the pie series `arcLabel` ('label'/'value'/callback). Unmappable configs (format strings, multi-line arrays) are `partial` gaps.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tooltip`                  | ⚠️ ignored                                                | Declared in the type but never read by the compiler. `<ChartsTooltip />` is always mounted by `VegaLiteChart.tsx` and shows x-charts' own default series/point tooltip regardless of this channel; the spec's `tooltip` field list is not consulted to customize its content.                                                                                                                                                                                                                                                                                                                                                                      |
| `href`                     | ❌ unsupported (all tiers)                                | Declared in the type but never read anywhere; no gap is fired for it either. Workaround: use external click handlers on the chart container.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `key`                      | ⚠️ ignored                                                | Declared in the type but never read; data is matched by array index (via `categoryIndex`/`categoryKey` on the resolved axis field), not by `key`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `facet` / `row` / `column` | ✅ supported (wrapper)                                    | `<VegaLiteChart />` expands facet channels into a CSS grid of sub-charts (`src/facet`): rows partitioned per facet value, shared quantitative domains + category order injected, per-facet headers. The pure `compileSpec` API still gaps composite specs (render through the component).                                                                                                                                                                                                                                                                                                                                                          |

---

## Data & transforms

### Data source

| Feature                                           | Status                     | Notes                                                                                                                                                                                                     |
| :------------------------------------------------ | :------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Inline values (`data.values` as object array)     | ✅ supported               | `normalize/index.ts` `resolveRows`.                                                                                                                                                                       |
| Named datasets (`data.name` + `datasets` map)     | ✅ supported               | Resolved via the `datasets` prop (host-provided) merged over `spec.datasets`, host takes precedence; a missing name reports an `unsupported` gap (`data:named-missing`) and falls back to inherited rows. |
| URL loading (`data.url`)                          | ❌ unsupported (all tiers) | `unsupported` gap (`data:url`); no HTTP fetching in the wrapper. Workaround: fetch data yourself and pass via the `data` prop or `datasets`.                                                              |
| String payloads (`data.values` as CSV/TSV string) | ❌ unsupported (all tiers) | `unsupported` gap (`data:string-values`); string parsing not implemented. Workaround: parse to a JSON array first.                                                                                        |
| `data.format` (CSV parsing, delimiter, etc.)      | ⚠️ ignored                 | Declared in the `VegaData` type but never read in `normalize/index.ts`; no gap is fired for it. Data must already be parsed into row objects.                                                             |

### Transforms

Transforms are applied sequentially in order (`transforms/index.ts` `applyTransforms`, plus the inline encoding-level pass `applyEncodingTransforms` in `transforms/encoding.ts` for `aggregate`/`bin`/`timeUnit` declared directly on a channel field def). Unrecognized top-level transform kinds are skipped with a gap.

| Transform       | Status                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| :-------------- | :------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `aggregate`     | ✅ supported               | `transforms/aggregate.ts` (top-level) and the inline pass in `transforms/encoding.ts`, sharing the op evaluator in `transforms/aggregateOps.ts`. Full group-by aggregation: `count`, `valid`, `missing`, `distinct`, `sum`, `product`, `mean`/`average`, `min`, `max`, `median`, `q1`, `q3`, `variance`, `variancep`, `stdev`, `stdevp`, `stderr`. `argmin`/`argmax` are recognized but not implemented (`unsupported` gap, `aggregate:argminmax`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `fold`          | ✅ supported               | Inline in `transforms/index.ts` `applyTransforms` (not a separate module). Pivots columns to rows (key–value pairs); `fold.as` defaults to `['key', 'value']`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `filter`        | ✅ supported               | `transforms/filter.ts`. Field predicates (`{field, equal/lt/lte/gt/gte/range/oneOf/valid}`, optionally with a `timeUnit` qualifier — compared against the raw value with a `partial` gap since it isn't actually truncated first), logical composition (`and`/`or`/`not`, three-valued to fail open on unsupported sub-predicates), and Vega expression strings (via the shared safe evaluator in `transforms/calculate.ts` — **never** `eval()`/`new Function()`). Selection/param predicates and malformed shapes report an `unsupported` gap and keep every row (fail open).                                                                                                                                                                                                                                                                                                                                                                                                         |
| `calculate`     | ✅ supported               | `transforms/calculate.ts`. A hand-rolled recursive-descent parser/evaluator (never `eval()`) for a safe expression subset: arithmetic, string concatenation, comparisons, ternaries, `&&`/\|\|/`!`, `datum.field`/`datum['field']` access, and an allow-listed function set (`abs`, `round`, `floor`, `ceil`, `sqrt`, `min`, `max`, `length`, `upper`, `lower`, `toNumber`, `toString`, `year`, `month`, `date`). Syntax/semantics outside this subset report an `unsupported` gap (`transform:calculate`) and leave the target field `null`.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `bin`           | ✅ supported               | `transforms/bin.ts` (top-level `applyBinTransform`, and inline `applyInlineBin` wired from `transforms/encoding.ts`). d3-style "nice" binning honoring `maxbins` (default 10), an explicit `step`, and an explicit `extent` (values outside an _explicit_ extent are excluded; a _derived_ extent always covers every value). The top-level transform writes `as` (bin start) and `as`+`_end` (or `as[1]`); the inline (encoding-channel) form writes a synthetic `"start–end"` ordinal label column so histograms go through the existing band-scale axis path. `bin: "binned"` (pre-binned data) is recognized but not implemented (`partial` gap, `encoding:bin-binned`) and falls back to the raw unbinned field.                                                                                                                                                                                                                                                                   |
| `timeUnit`      | ✅ supported               | `transforms/timeUnit.ts` (top-level `applyTimeUnitTransform`, and inline `applyInlineTimeUnit`). Calendar truncation for `year`, `quarter`, `month`, `week`, `date`, `hours`, `minutes`, `seconds`, `milliseconds`, and the composites `yearquarter`/`yearmonth`/`yearmonthdate`/`yearweek`/`monthdate`/`hoursminutes`/`hoursminutesseconds` (each truncates to its finest named component while keeping the real calendar date — a documented simplification vs. Vega-Lite's cross-year cyclic alignment for a single unit like `month`). The cyclic `day` (day-of-week) unit is supported but always reports a `partial` gap (`timeUnit:day`) since it maps onto a synthetic reference week, not the real date. Units outside this set (e.g. `dayofyear`) report an `unsupported` gap (`timeUnit:<unit>`) and pass rows through with a `null`/unwritten field.                                                                                                                        |
| `window`        | ✅ supported               | `transforms/window.ts`. Ranking ops (`row_number`, `rank`, `dense_rank`, `percent_rank`, `cume_dist`, `ntile`), offset ops (`lag`/`lead`), value ops (`first_value`/`last_value`/`nth_value`), and every `aggregate`-family op (via the shared `evaluateAggregate`) over a sorted, partitioned, peer-group-aware frame (SQL `RANGE`-style peer snapping unless `ignorePeers: true`). **Deviation:** Vega-Lite reorders rows into sort order as its output; this wrapper computes over the sorted view internally but writes results back in the caller's ORIGINAL row order, so a `window` step never corrupts other channels' row-index alignment. `ntile`/`nth_value` with a missing/invalid `param` report an `unsupported` gap (`window:ntile-param`/`window:nth-value-param`) and null the output; a truly unrecognized op reports `window:op:<op>`.                                                                                                                               |
| `joinaggregate` | ✅ supported               | `transforms/joinaggregate.ts`. Same group-by aggregation as `aggregate` (sharing `evaluateAggregate`), joined back onto every row of the group instead of collapsing to one row per group — row count, order, and existing fields are preserved. Unimplemented ops report an `unsupported` gap (`aggregate:<op>`) and null the field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `stack`         | ❌ unsupported (all tiers) | Explicit stacking transform not needed; x-charts bar/line infer `stack` from encoding. Use the encoding `stack` property instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `lookup`        | ✅ supported (inline)      | `transforms/lookup.ts` — first-match join on `from.key` with `fields`/`as` renaming, whole-datum-under-`as`, `default` for non-matches, and dotted-path keys on GeoJSON features (choropleth joins). `from.data` must be inline `values`; `url`/named datasets are gaps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pivot`         | ❌ unsupported (all tiers) | Column pivoting has no x-charts equivalent. Workaround: use `fold` or pivot data in preprocessing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `density`       | ✅ supported               | `transforms/density.ts`. Gaussian KDE per group over a FIXED, evenly spaced grid of `steps + 1` points (default `steps` clamped between `minsteps`/`maxsteps` around 100). Bandwidth defaults to a Scott's-rule estimator (`1.06 · min(stdev, IQR/1.34) · n^(-1/5)`, guarded against a zero spread); `cumulative` uses a Gaussian CDF (Abramowitz–Stegun `erf` approximation); `counts` scales the curve by the group size. **Deviation:** vega-statistics uses an adaptive grid; this wrapper always samples a fixed evenly spaced grid — no gap is reported since the output is numerically close and visually indistinguishable for smooth densities.                                                                                                                                                                                                                                                                                                                                |
| `regression`    | ✅ supported               | `transforms/regression.ts`. Closed-form OLS fits: `linear` (direct), `log`/`exp`/`pow` (log-linearized OLS — see deviation below), `quad` (degree-2 polynomial), `poly` (degree `min(order ?? 3, pairs - 1)` via a Vandermonde normal-equations solve + Gaussian elimination with partial pivoting). Emits a sampled curve by default (2 endpoints for `linear`, 50 points otherwise, over `extent` or the data's x-range) or one `{coef, rSquared}` summary row per group with `params: true`. Domain violations (`log`/`pow` need `on` > 0; `exp`/`pow` need `regression` > 0) drop the offending rows with a `partial` gap (`transform:regression-domain`); an unrecognized `method` falls back to `linear` with a `partial` gap (`transform:regression-method`). **Deviation:** `exp`/`pow` are fit by log-linearizing (OLS in log-space) rather than vega-statistics' direct nonlinear least squares — exact for noiseless data, slightly biased vs. y-space error for noisy data. |
| `loess`         | ✅ supported               | `transforms/loess.ts`. Full LOESS (Cleveland's algorithm): a tricube-weighted linear fit at each point over its `span` (`max(2, ceil(bandwidth · n))`, default `bandwidth: 0.3`) nearest x-neighbors, refined by two bisquare-weighted robustness iterations. Groups with fewer than 3 valid points pass through unsmoothed (no gap — too little data to fit, not a translation failure).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `impute`        | ❌ unsupported (all tiers) | Missing-value imputation has no x-charts equivalent. Workaround: impute before passing data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `flatten`       | ❌ unsupported (all tiers) | Nested array flattening has no x-charts equivalent. Workaround: flatten before passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `sample`        | ❌ unsupported (all tiers) | Stochastic row sampling has no x-charts equivalent. Workaround: sample before passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `quantile`      | ✅ supported               | `transforms/quantile.ts`. Per-group quantile curves reusing the shared linear-interpolation `quantile()` helper (the same one behind the `median`/`q1`/`q3` aggregate ops). `probs` defaults to a generated series from `step` (default `0.01`): `step/2, step/2 + step, …` while `< 1`. Groups with no numeric values emit nothing (not a gap — an empty curve for an empty group is expected).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

All of the remaining `KNOWN_UNSUPPORTED_TRANSFORMS` (`transforms/index.ts`: `pivot`, `sample`, `stack`, `impute`, `flatten`) plus any other unrecognized top-level transform key report `transform:<kind>` as an `unsupported` gap and are skipped, leaving downstream rows unmodified by that step.

---

## Scales & axes

### Scale types

`compile/scales.ts` `resolveChannelAxis` resolves positional (x/y) scales. Temporal data is approximated as a discrete band/point scale over the sorted date domain (a `partial` gap, `scale:temporal-point-approximation`, is always reported for a temporal channel).

| Scale type                                           | Status       | Notes                                                                                                                                                                                                                                                                                      |
| :--------------------------------------------------- | :----------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `linear`                                             | ✅ supported | Default for quantitative axes.                                                                                                                                                                                                                                                             |
| `log`                                                | ✅ supported | Logarithmic scale.                                                                                                                                                                                                                                                                         |
| `pow`                                                | ⚠️ partial   | Scale type maps through, but `scale.exponent` is declared in the `VegaScale` type and never read by `resolveChannelAxis` — the x-charts default exponent is always used, not the spec's value.                                                                                             |
| `sqrt`                                               | ✅ supported | Square-root scale.                                                                                                                                                                                                                                                                         |
| `symlog`                                             | ⚠️ partial   | Scale type maps through, but `scale.constant` (the symlog linear-region constant) is never read — the x-charts default is always used.                                                                                                                                                     |
| `time` / `utc`                                       | ⚠️ partial   | Not a continuous time scale: approximated as a discrete band/point scale over the sorted `Date` domain (see above).                                                                                                                                                                        |
| `band`                                               | ✅ supported | Categorical; used by default for bar/rect marks (`channelHasBarMark`).                                                                                                                                                                                                                     |
| `point`                                              | ✅ supported | Categorical; used by default for non-bar marks.                                                                                                                                                                                                                                            |
| `ordinal`                                            | ✅ supported | An explicit `scale.type: 'ordinal'` maps to x-charts' `band` scale (`explicitDiscreteScaleType`), matching Vega's own convention that an ordinal position scale is a band scale. (Not "treated as point" — that was true of a prior implementation but is not what the current code does.) |
| Unsupported types (e.g., `sequential`, custom names) | ⚠️ partial   | An unrecognized `scale.type` string falls back to `linear` with a `partial` gap (`scale:<type>`) — the chart still renders (with the wrong scale semantics), it does not drop the axis.                                                                                                    |

Band-scale `padding`/`paddingInner`/`paddingOuter` (Vega-Lite spacing knobs) are declared in the `VegaScale` type but never read anywhere in `compile/scales.ts` — see the Scale properties table below.

### Scale properties

| Property                                    | Status       | Notes                                                                                                                                                                                                                                                                                                                   |
| :------------------------------------------ | :----------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `domain`                                    | ✅ supported | Quantitative: `[min, max]` (numeric entries only) → axis `min`/`max`. Categorical: auto-collected from data across every layer sharing the channel, in first-appearance order (or `sort` order — see Axis configuration).                                                                                               |
| `range`                                     | ⚠️ ignored   | On a _positional_ (x/y) channel: never read in `compile/scales.ts`. (On the `color` channel, `scale.range` IS read — see the Encoding channels `color` row.)                                                                                                                                                            |
| `scheme`                                    | ⚠️ ignored   | Same distinction as `range`: irrelevant/unread on positional channels; read on `color` (Encoding channels table).                                                                                                                                                                                                       |
| `zero`                                      | ⚠️ ignored   | Declared in the type; a code comment in `compile/scales.ts` notes the x-charts default already lets a bar-anchored axis float from zero, but there is no branch that actually reads `scale.zero`.                                                                                                                       |
| `nice`                                      | ⚠️ partial   | Only the `false` value has an effect: `scale.nice === false` sets the quantitative axis's `domainLimit` to `'strict'` (an exact, non-rounded domain). `true` (the Vega-Lite default) or an explicit tick-count number are not distinguished from "unset" — the x-charts default (non-strict) domain is used either way. |
| `reverse`                                   | ✅ supported | `scale.reverse === true` sets `reverse: true` on the axis config, for both categorical and quantitative channels.                                                                                                                                                                                                       |
| `padding` / `paddingInner` / `paddingOuter` | ⚠️ ignored   | Declared in the `VegaScale` type but never read anywhere in `compile/scales.ts`; band-scale spacing always uses the x-charts axis default, regardless of what the spec requests.                                                                                                                                        |

### Axis configuration

All of the below is read from a field def's `axis` object (`buildAxisExtras` / `axisTitle` in `compile/scales.ts`); a bare `axis: null` on the field def removes the axis entirely (maps to x-charts' `position: 'none'`, which reserves layout space but hides the axis — a real, working x-charts feature).

| Axis feature                    | Status                | Notes                                                                                                                                                                                                                                                                    |
| :------------------------------ | :-------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`                         | ✅ supported          | Explicit `axis.title` (including `null` to suppress it) wins, then the field def's own `title`, then an auto-derived name. Encoding-level aggregates derive a human-readable title during the rewrite (`transforms/encoding.ts`), e.g. "MEAN of v" / "Count of Records". |
| `labels`                        | ⚠️ partial            | Only `labels: false` has an effect (`tickLabelStyle.display = 'none'`); there is no handling for `labels: true` beyond the (already-default) visible state.                                                                                                              |
| `ticks`                         | ⚠️ ignored            | Declared in the `VegaAxis` type but never read by `buildAxisExtras`; tick marks always render via the x-charts default regardless of `axis.ticks: false`.                                                                                                                |
| `grid`                          | ✅ supported          | `resolveAxes` sets `grid.vertical`/`grid.horizontal` from `encoding.x.axis.grid`/`encoding.y.axis.grid`; `<ChartsGrid>` renders accordingly.                                                                                                                             |
| `orient`                        | ⚠️ partial            | `mapOrient` accepts `'top'`/`'bottom'` for `x` and `'left'`/`'right'` for `y` (the other pair is silently dropped, not just "may be overridden").                                                                                                                        |
| `format`                        | ⚠️ ignored (reported) | A string `axis.format` (d3-format / d3-time-format) always reports a `partial` gap (`scale:axis-format`) and is never applied; x-charts' own default number/date formatting is used unconditionally.                                                                     |
| `tickCount`                     | ⚠️ partial            | Passed straight through as `tickNumber`; x-charts treats it as a hint, not an exact count.                                                                                                                                                                               |
| `values`                        | ⚠️ partial            | Passed straight through as `tickInterval`; x-charts may still adjust what's actually rendered.                                                                                                                                                                           |
| `labelAngle`                    | ⚠️ partial            | Passed straight through as `tickLabelStyle.angle`; no gap reported (present since the mapping is direct), but x-charts may override for collision avoidance.                                                                                                             |
| `domain` (axis-line visibility) | ⚠️ ignored            | Declared in the `VegaAxis` type but never read by `buildAxisExtras`; the axis line always renders via the x-charts default. (Not to be confused with `scale.domain`, the data-domain array, which IS supported — see Scale properties.)                                  |
| Full axis removal               | ✅ supported          | `axis: null` on the field def → `position: 'none'` (see the note above the table). There is no separate `axis.disable` property in Vega-Lite or in this wrapper's `VegaAxis` type; `axis: null` is the correct/only way to hide an axis.                                 |

---

## Projections & geographic

| Feature                                  | Status                 | Notes                                                                                                                                                                                          |
| :--------------------------------------- | :--------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| All projections (mercator, albers, etc.) | ❌ unsupported (MIT)   | Geographic projections need `@mui/x-charts-premium` `Map` chart. Gap: projection config ignored; geojson data has no sink.                                                                     |
| `geojson` field type                     | ❌ unsupported (MIT)   | GeoJSON parsing and rendering need premium Map. Gap: geojson fields reported as unsupported.                                                                                                   |
| `geoshape` mark                          | ✅ supported (Premium) | See Marks section — named d3 projections map through (`mercator` default); `rotate`/`scale`/`translate` forward to the provider; `center`/`parallels`/`clipAngle`/`fit` remain `ignored` gaps. |

---

## View composition & layering

| Feature                                               | Status                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| :---------------------------------------------------- | :---------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `layer`                                               | ✅ supported                  | `normalize/index.ts` `walk()` recursively flattens `layer` arrays into a flat unit list, inheriting parent `encoding`/`transform`/`data` down the tree. Every layer's marks/series render into one shared `<ChartsSurface>` (overlapping), and axis domains are unioned across every layer that shares a positional channel (categorical: literal union of distinct values; quantitative: only the first occurrence's `scale.domain`/type is read — see Scales & axes). |
| `facet`                                               | ✅ supported (wrapper)        | The `facet` operator ({row, column} matrix or {field}+`columns` wrapping grid) expands into a grid of sub-charts with shared scales. Facet `sort`/header styling config not honored (`partial`); depth capped at 2.                                                                                                                                                                                                                                                     |
| `repeat`                                              | ❌ unsupported (all tiers)    | Not present in the `VegaLayerSpec`/`VegaUnitSpec` grammar this wrapper parses at all — a `repeat` spec has no `mark`/`layer` key, so `normalizeSpec` produces zero units and no series, silently. Workaround: render multiple charts in your app.                                                                                                                                                                                                                       |
| `hconcat` / `vconcat` / `concat`                      | ✅ supported (wrapper)        | Each entry renders as an independent sub-chart in a CSS grid (independent scales, per Vega-Lite's concat default), inheriting top-level data when it declares none.                                                                                                                                                                                                                                                                                                     |
| `resolve.scale.x` / `resolve.scale.y`                 | ✅ supported (detection only) | `compile/scales.ts` `resolveAxes` reports an `unsupported` gap (`resolve:independent-scale`) when `resolve.scale.x` or `resolve.scale.y` is `'independent'` — the shell always renders one shared x/y axis pair, so every layer is forced to share the resolved scale regardless of the request. `'shared'` (the default) needs no special handling: the axis-domain union described in the `layer` row above already happens unconditionally.                          |
| `resolve.scale.color` / `.size` / `.shape` / `.theta` | ⚠️ ignored                    | Declared in the `VegaLayerSpec['resolve']['scale']` type but never read — only `x`/`y` are checked in `resolveAxes`'s `for (const positional of ['x', 'y'] as const)` loop.                                                                                                                                                                                                                                                                                             |
| `resolve.axis`                                        | ⚠️ ignored                    | Declared in the type but never read anywhere in the compiler; independent-axis requests produce no gap and have no effect (axes are always shared, per the `layer` row above).                                                                                                                                                                                                                                                                                          |
| `resolve.legend`                                      | ⚠️ ignored                    | Declared in the type but never read anywhere; legend merging/splitting requests have no effect — the single shared `<ChartsLegend>` always reflects every series' own label/color.                                                                                                                                                                                                                                                                                      |

---

## Interactivity & selections

| Feature                          | Status                     | Notes                                                                                                                                                                                                                                                                                                                                                                          |
| :------------------------------- | :------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `params` / selections            | ⚠️ partial                 | `compile/params.ts`: point selections (`select: 'point'`) enable x-charts item hover-highlighting (`highlightScope {highlight:'item', fade:'global'}` on every series); refinements (`on`/`toggle`/`fields`/`encodings`/`nearest`) are `ignored`. Interval selections, selection-driven conditional encodings, and cross-view filtering remain gaps (`partial`/`unsupported`). |
| `bind` (inputs, scales, legends) | ❌ unsupported (all tiers) | Not part of the accepted spec grammar at all (no `bind` field in the types); data binding to selections/widgets is not supported. Workaround: use React state and re-render.                                                                                                                                                                                                   |
| Zoom & pan                       | ❌ unsupported (MIT)       | Vega-Lite zoom/pan (via `params`/selections) is not read at all (see above). `@mui/x-charts-pro` has `ChartZoomSlider` for chart zoom, but it implements pro's own zoom model, not Vega-Lite selection semantics — this wrapper does not wire it up.                                                                                                                           |
| Drag & brush                     | ❌ unsupported (all tiers) | Brush/drag selection has no x-charts primitive and is not read from the spec. Workaround: implement with React mouse events.                                                                                                                                                                                                                                                   |
| Tooltips (data encoding)         | ⚠️ ignored                 | The `tooltip` encoding channel is declared in the type but never read by any mark compiler (see the Encoding channels table). `<ChartsTooltip />` is always mounted and shows x-charts' own default series/point tooltip, independent of what the spec's `tooltip` field list would request.                                                                                   |
| Href (click)                     | ❌ unsupported (all tiers) | `href` is declared in the type but never read anywhere; no gap is fired for it. x-charts marks do not navigate on click. Workaround: use external click handlers on the chart container.                                                                                                                                                                                       |
| Context menu                     | ❌ unsupported (all tiers) | Not part of the Vega-Lite grammar this wrapper models; right-click interactions are not supported. Workaround: implement custom event handlers.                                                                                                                                                                                                                                |

---

## Config & theming

| Feature                      | Status                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                              |
| :--------------------------- | :------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Spec-level `config` blocks   | ⚠️ ignored                 | `spec.config` is declared in the `VegaLiteSpec` type but never read anywhere in the compiler (`config.mark`/`config.axis`/`config.legend` etc. all have no effect). The chart always uses the ambient MUI theme instead. Workaround: customize the MUI theme or per-mark properties (`mark.color`, etc., which ARE read).                                                                                          |
| Mark-level style properties  | ⚠️ partial                 | `mark.color`/`fill`/`stroke` are read for a static series color (bar/line/area/arc/rule); `mark.strokeWidth`/`strokeDash` are read only by `marks/rule.ts` (→ `lineStyle`) — on line/area marks they are recognized but explicitly `ignored` (`mark:strokeWidth`, `mark:strokeDash` gaps); `mark.opacity`/`fillOpacity`/`strokeOpacity` are recognized but always `ignored` (no series-level opacity in x-charts). |
| Axis config (via encoding)   | ✅ supported               | `encoding.x.axis` / `encoding.y.axis` applied per the Axis configuration table above.                                                                                                                                                                                                                                                                                                                              |
| Legend config (via encoding) | ⚠️ ignored                 | `encoding.color.legend` is read only for `legend: null` (suppresses the legend, honored via `hasLegend`); any other non-empty legend config object (e.g. `title`, `orient`) reports an `ignored` gap (`encoding:color-legend-config-ignored`) and has no effect — a single default `<ChartsLegend>` is always shown when any series has a label.                                                                   |
| Background & padding         | ⚠️ ignored                 | `spec.background`/`spec.padding` are declared in the type but never read anywhere; no gap is fired for them. Chart background/padding depend entirely on the container and x-charts defaults.                                                                                                                                                                                                                      |
| Autosize                     | ⚠️ ignored                 | `spec.autosize` is declared in the type but never read; `spec.width`/`spec.height` of `'container'` or a step/object form report an `ignored` gap (`size:width-step`/`size:height-step` in `normalize/index.ts`) and fall back to container sizing (the `width`/`height` props on `<VegaLiteChart>`, or the chart's natural container size).                                                                       |
| Font config                  | ❌ unsupported (all tiers) | Not part of the accepted spec grammar; x-charts applies MUI theme fonts unconditionally.                                                                                                                                                                                                                                                                                                                           |
| Renderer (SVG vs. Canvas)    | ⚠️ ignored                 | Not part of the accepted spec grammar (Vega-Lite's `config.mark.renderer` isn't in the `VegaLiteSpec` type at all); `<ChartsSurface>` always renders SVG.                                                                                                                                                                                                                                                          |

---

## Full Vega (non-Lite) specs

| Feature                                                | Status                     | Notes                                                                                                                                                                                                                                                                                                    |
| :----------------------------------------------------- | :------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vega signals/reactive operators                        | ❌ unsupported (all tiers) | Full Vega reactive model entirely out of scope. Signals, scales, axes, and marks are defined in an imperative/reactive grammar not expressible as x-charts series config. Gap: entire Vega spec rejected. Workaround: transpile Vega to Vega-Lite subset manually or use full Vega runtime (vega-embed). |
| Vega data operators (e.g., cross, sequence, graticule) | ❌ unsupported (all tiers) | Vega-specific data generators have no x-charts equivalent. Workaround: generate data in preprocessing.                                                                                                                                                                                                   |
| Vega marks (polygon, path, etc.)                       | ❌ unsupported (all tiers) | Vega's lower-level mark types not supported; only Vega-Lite marks mapped. Workaround: use SVG overlays.                                                                                                                                                                                                  |

---

## How gaps are reported at runtime

When the wrapper encounters untranslatable Vega-Lite features, it collects them as `TranslationGap` objects and returns them via the `onGaps` prop. Each gap has a `code` (e.g., `mark:geoshape`, `encoding:facet`), a human-readable `message` (often including workarounds and tier pointers), a `severity` (unsupported/partial/ignored), and optionally a `path` into the input spec (e.g., `layer[0].encoding.color`).

Example usage:

```tsx
import { VegaLiteChart } from '@mui/x-charts-vega';

const spec = {
  mark: 'geoshape',
  // ...
};

const [gaps, setGaps] = useState([]);

<VegaLiteChart spec={spec} onGaps={setGaps} />;

// gaps[0].code === 'composition:repeat'
// gaps[0].message === '`repeat` view composition has no x-charts equivalent and is not expanded…'
// gaps[0].severity === 'unsupported'
```

Render gaps as warnings in your UI, or use them to fall back to a different visualization. The wrapper always attempts a best-effort render even when gaps are present.
